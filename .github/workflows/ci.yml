name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Run tests
        run: >-
          uv run --frozen --extra dev --python ${{ matrix.python-version }}
          python -m pytest -q --cov=app --cov-report=term-missing

  lint:
    name: lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v5
      - name: Set up Python
        run: uv python install 3.11
      - name: Ruff check
        run: uv run --frozen --extra dev --python 3.11 ruff check app benchmarks

  types:
    # Non-blocking: HELIOS carries known typing debt outside the contracts/core
    # boundary. This job surfaces regressions without gating merges. Remove
    # `continue-on-error` once the debt is burned down.
    name: types (mypy, informational)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v5
      - name: Set up Python
        run: uv python install 3.11
      - name: Mypy
        continue-on-error: true
        run: uv run --frozen --extra dev --python 3.11 python -m mypy app
